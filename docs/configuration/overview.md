# Configuration overview

!!! info "Under construction"

## What this page will cover

- The 12 config sections in WSTerminal
- Tier differences — Basic edits position sizing + symbol/tick;
  Pro edits everything else
- The two shipping presets — WS Terminal Standard (recommended
  default, drives the published equity curve) and WS Terminal Max
  (higher-PnL alternative)
- User-saved presets (Pro) — save / load / rename / delete your
  own configurations
- Where the config persists, what survives an update, what doesn't
- Live editing while the bot is running
